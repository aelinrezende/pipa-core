/**
 * Namespaces de customTypes de tool-result (`feature.<action>`): o nudge duplica o
 * toolResult que o execute() já retorna (`{ content, details }`) — removido pelo
 * ReminderCleaner quando o toolResult do mesmo tool call está no contexto.
 * Extensível: nova feature adiciona seu namespace aqui (CA-9).
 */
export declare const TOOL_RESULT_NAMESPACES: ReadonlySet<string>;
//# sourceMappingURL=reminder.d.ts.map